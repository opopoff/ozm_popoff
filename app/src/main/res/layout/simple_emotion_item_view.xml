<?xml version="1.0" encoding="utf-8"?>
<com.ozm.rocks.ui.emotions.SimpleEmotionItemView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:gravity="center_horizontal|top"
    android:orientation="vertical"
    android:padding="2dp">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            >

            <FrameLayout
                android:id="@+id/simple_emotion_image_frame"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <ImageView
                    android:id="@+id/simple_emotion_image"
                    android:layout_width="@dimen/simple_emotion_image_size"
                    android:layout_height="@dimen/simple_emotion_image_size"
                    android:adjustViewBounds="true"
                    android:scaleType="centerCrop"
                    />
            </FrameLayout>

            <ProgressBar
                android:id="@+id/progress"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"/>
        </FrameLayout>


        <ImageView
            android:id="@+id/simple_emotion_promo_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right|top"
            android:background="@drawable/promo_label_bg"
            android:src="@drawable/ic_promo_label"
            android:visibility="gone"
            />

    </FrameLayout>

    <TextView
        android:id="@+id/simple_emotion_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:background="@drawable/emotion_label_bg"
        android:gravity="center"
        android:textColor="@color/primary_text"/>


</com.ozm.rocks.ui.emotions.SimpleEmotionItemView>