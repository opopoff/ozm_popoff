<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/sharing_dialog_header_bg"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/choose_dialog_header_image"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingLeft="16dp"
            android:paddingRight="44dp" />

        <TextView
            android:id="@+id/choose_dialog_header_text"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:gravity="center_vertical|left"
            android:text="@string/sharing_dialog_header"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            app:customTypeface="regular"
            />
    </LinearLayout>

    <GridView
        android:id="@+id/choose_dialog_grid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:listSelector="@drawable/orange_selector"
        android:numColumns="3" />
</LinearLayout>